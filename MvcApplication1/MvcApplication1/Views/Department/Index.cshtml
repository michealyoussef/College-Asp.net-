@model IEnumerable<MvcApplication1.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AcademicYear.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AcademicYear.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.DepartmentID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.DepartmentID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.DepartmentID }) |
                    @Html.ActionLink("Accademic Staff", "GetAcademicStaff", new { id = item.DepartmentID, Yer = item.AcademicYearID }) |
                    @Html.ActionLink("Section", "GetSections", new { id = item.DepartmentID, Yer = item.AcademicYearID }) |
                    @Html.ActionLink("Courses", "GetCourses", new { id = item.DepartmentID, Yer = item.AcademicYearID }) |
                    @Html.ActionLink("Student", "GetStudents", new { id = item.DepartmentID, Yer = item.AcademicYearID })
                </td>
            </tr>
        


    }

</table>
